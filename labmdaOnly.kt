fun main(){
    var mylambda: (Int) -> Unit = {s:Int -> println(s)}
    addnumber(myLambda)
}
fun addnumber(mylambda:(Int)->Unit){ //-skip-lambda-
    var add = 5
    myLambda(add)
    // can't under_stand Lambda'$ any-Way
}